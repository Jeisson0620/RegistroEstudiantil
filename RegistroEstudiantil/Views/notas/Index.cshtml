@model IEnumerable<RegistroEstudiantil.Models.notas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IDProfesores)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estudiantes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IDProfesores)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estudiantes.IDEstudiantes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDNota">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDNota">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDNota">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
